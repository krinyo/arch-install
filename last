#set-vmvideo arch -horizontalresolution:1920 -verticalresolution:1080 -resolutiontype single

pacman -S xorg-xinit xorg-server i3 awk coreutils i3lock imagemagick util-linux git gnome-screenshot maim scrot wmctrl polybar picom i3-gaps chromium

echo "i3" >> .xinitrc

echo "if [[ -z $DISPLAY ]] && [[ $(tty) = /dev/tty1 ]]; then
startx
fi" >> .bash_profile
